# MUSUBI Project Configuration
# Auto-generated by musubi-onboard on 2026-02-19

project_name: "maruhuku-contents"
description: "技術記事のコンテンツ管理"
version: "1.0.0"

# Detected languages
languages:
  - typescript
  - markdown
  - yaml
  - json

# Detected frameworks
frameworks:
  - name: "Unknown"
    version: "0.0.0"

# Project structure
conventions:
  architecture_pattern: "unknown"
  directory_structure:
    video-projects: "video-projects"
    storage: "storage"
    steering: "steering"
    raws: "raws"
    plans: "plans"

# Steering configuration
steering:
  auto_update:
    enabled: false
    frequency: "on-demand"
  
  excluded_paths:
    - "node_modules/**"
    - "dist/**"
    - "build/**"
    - ".git/**"
    - "*.log"
  
  memories:
    enabled: true
    path: "steering/memories/"
    max_file_size_kb: 500
    retention_days: 365

# Agent configuration
agents:
  default_language: "ja"
  bilingual_output:
    enabled: true
    languages: ["en", "ja"]
  
  output:
    gradual_generation: true
    progress_indicators: true
    large_file_splitting: true
    split_threshold_lines: 300

# Development workflow
workflow:
  testing:
    required: false
    coverage_threshold: 80
  
  quality_gates:
    - name: "lint"
      command: "npm run lint"
      required: false

# Custom rules (adjust as needed)
custom_rules:
  - "All development shall follow specification-first approach"
  - "All code shall maintain high quality standards"
  - "Documentation shall be kept up to date"

# Generated by musubi-onboard
# Review and customize as needed
