---
title: "マルチエージェントAI開発の現実 — tmux+YAML手動オーケストレーション vs 統合プラットフォーム実践比較"
emoji: "⚔️"
type: "tech"
topics: ["Claude", "AI", "マルチエージェント", "自動化", "開発効率"]
published: false
---

## はじめに

「AIエージェントを複数並列で動かせば開発が爆速になる」——そう考えて、マルチエージェントオーケストレーションシステムを構築した。

結論から言うと、**手動で組んだシステムは2週間で役目を終えた**。Claude Code本体にオーケストレーション機能が入り、統合プラットフォーム（OpenClaw）との組み合わせが上位互換になったからだ。

しかし、この「失敗から学んだ知見」にこそ価値がある。本記事では、同じマルチエージェント構造を**2つの方式で実装した実体験**をもとに、設計判断の勘所と泥臭い現実を共有する。

:::message
本記事は[「AIに仕事を任せる技術」](https://zenn.dev/imudak/articles/ai-autonomous-dev-practice)の続編的位置づけです。前回は「司令塔AI + マルチエージェント」の概要を紹介しましたが、今回はオーケストレーション層にフォーカスします。
:::

## 比較対象

### A. multi-agent-shogun（手動オーケストレーション方式）

[yohey-w/multi-agent-shogun](https://github.com/yohey-w/multi-agent-shogun)をforkし、独自設定を加えたシステム。tmux上で複数のClaude Codeインスタンスを戦国時代の軍制になぞらえて管理する。

```
        殿（人間）
           │
           ▼
    ┌──────────────┐
    │  将軍(SHOGUN) │  ← Claude Code #1: 統括・戦略
    └──────┬───────┘
           │ YAMLファイル + tmux send-keys
    ┌──────▼───────┐
    │  家老(KARO)   │  ← Claude Code #2: タスク分解・管理
    └──────┬───────┘
           │
  ┌─┬─┬─┬─┴─┬─┬─┬─┐
  │1│2│3│4│5│6│7│8│  ← Claude Code #3〜10: 実行部隊
  └─┴─┴─┴─┴─┴─┴─┴─┘
       足軽(ASHIGARU)
```

**起動:** bashスクリプトでtmuxセッション作成 → 9ペイン展開 → 全インスタンスにClaude Code起動 → 各自の指示書を読み込ませる。

### B. 現行フロー（統合プラットフォーム方式）

OpenClaw上の司令塔AI（メインセッション）が、`sessions_spawn`やClaude Code CLIを必要に応じて起動する方式。

```
        殿（人間）
           │
           ▼
    ┌──────────────┐
    │  司令塔AI     │  ← OpenClawメインセッション
    └──────┬───────┘
           │ sessions_spawn / claude -p
     ┌─────┼─────┐
     ▼     ▼     ▼
   [調査] [実装] [分析]  ← 必要時のみ起動、完了後消滅
```

## 泥臭い知見：手動オーケストレーションで何が起きたか

### 1. tmux send-keysの罠

shogunシステムで最も苦労したのが、エージェント間通信だ。

```bash
# ❌ これが動かない
tmux send-keys -t multiagent:0.0 'タスクを実行せよ' Enter

# ✅ 2回に分けないとEnterが正しく解釈されない
tmux send-keys -t multiagent:0.0 'タスクを実行せよ'
tmux send-keys -t multiagent:0.0 Enter
```

たった1行のコマンドが動かない。原因はtmuxのキー送信タイミングとClaude Codeの入力バッファの相性問題だ。指示書に「**絶対禁止パターン**」「**2回に分けろ**」と赤文字で書かざるを得なかった。

**統合プラットフォームでは？** → API経由で通信するため、この問題は存在しない。

### 2. 「待機できない」問題

Claude Codeは対話型AIだ。プロンプトが表示されている状態は「待機中」ではなく「停止中」。つまり：

```
❌ 家老が足軽にタスクを振って「報告を待つ」
  → 家老のClaude Codeは停止。足軽がsend-keysしても処理できない

✅ 「起こされたら全確認」方式
  → 足軽がsend-keysで起こす → 家老が全報告ファイルをスキャン → 次のアクション
```

これはClaude Codeをマルチエージェントで使う上での根本的な制約だ。ポーリング（定期確認ループ）はAPI代金の浪費になるため禁止。結果、非同期イベント駆動にならざるを得ない。

**統合プラットフォームでは？** → heartbeat機能とcronジョブで自然に解決。待機コストゼロ。

### 3. レースコンディション地獄

8つの足軽が並列で動くと、ファイル競合が発生する。

```
❌ 足軽1 → output.md に書き込み中
   足軽2 → output.md に書き込み → 上書き！

✅ 各自専用ファイルに分離
   足軽1 → output_1.md
   足軽2 → output_2.md
```

shogunでは「RACE-001」というルールIDまで振って、同一ファイルへの書き込みを禁止した。競合リスクがある場合は即座にblocked報告を上げさせる。

**統合プラットフォームでは？** → `sessions_spawn`は隔離セッションで動作するため、根本的にファイル競合が発生しない。

### 4. エラーハンドリングの3層構造

shogunでは、エラー発生時のエスカレーションパスを明確に定義した。

| ステータス | 意味 | 対処 |
|-----------|------|------|
| `done` | 正常完了 | 戦果テーブルに記録 |
| `failed` | 技術的失敗 | リトライ可否判断 → 別足軽に再割当 or エスカレーション |
| `blocked` | 人間判断必要 | dashboard.md「🚨要対応」セクションに記載 |

足軽 → 家老（リトライ判断）→ 将軍（殿への報告）→ 殿（最終判断）と、4段階のエスカレーションパスがある。

**統合プラットフォームでは？** → 司令塔AIが直接判断する。中間層がないため迅速だが、判断の品質は司令塔の能力に依存する。shogunの3層構造は、エージェント数が多い場合のフィルタリングとして合理的な設計だった。

### 5. 非同期ステータス共有の工夫

shogunで最も秀逸だったのが、`dashboard.md`による非同期ステータス共有だ。

```markdown
## 🚨 要対応 - 殿のご判断をお待ちしております
### ライセンス選択【判断必要】
- MIT / GPL / Apache のいずれか

## 🔄 進行中
| 足軽 | タスク | 進捗 |
|------|--------|------|
| 足軽1 | API調査 | 実行中 |
| 足軽3 | テスト作成 | 実行中 |

## ✅ 本日の戦果
| 時刻 | 任務 | 結果 |
|------|------|------|
| 14:15 | DB設計 | ✅ 完了 |
```

**更新は家老のみ**（単一責任で競合防止）。将軍も足軽もdashboard.mdを更新しない。この制約が、情報の一貫性を保証した。

**統合プラットフォームでは？** → HEARTBEAT.mdが似た役割を果たすが、構造化の度合いではshogunのdashboard.mdに学ぶべき点がある。エージェント数が増えた際の拡張パターンとして記録に値する。

## 定量比較

| 観点 | shogun（手動） | 現行フロー（統合） | 
|------|---------------|-------------------|
| セットアップ | bashスクリプト+tmux9ペイン | OpenClaw設定ファイルのみ |
| 常駐インスタンス | 10（将軍+家老+足軽8） | 1（司令塔のみ） |
| 並列度 | 8固定 | 動的（必要数だけ起動） |
| タスク受け渡し | YAMLファイル経由 | API直接呼び出し |
| 通信安定性 | send-keys依存（不安定） | API経由（安定） |
| トークン効率 | 全インスタンス常駐で消費 | 必要時のみ消費 |
| エラー伝播 | 3層（足軽→家老→将軍） | 1層（直接判断） |
| ファイル競合 | ルールで回避（RACE-001） | 隔離セッションで根絶 |
| 記憶機構 | Memory MCP（知識グラフ） | ファイルベース（MEMORY.md） |

## なぜshogunは2週間で役目を終えたか

### Claude Codeオーケストレーション機能の登場

2026年2月、Claude Code本体にagent teams / orchestration機能が追加された。これにより：

- **将軍の役割** → Claude Codeが内蔵
- **家老の役割** → タスク分解もClaude Codeが自動で行う
- **足軽の役割** → サブエージェントとして自動起動

tmux + YAMLで手動構築していた仕組みが、コマンド一発で実現できるようになった。

### 統合プラットフォームの成熟

OpenClawの`sessions_spawn`は、shogunが解決しようとした問題をより上位の抽象度で解決している。

```
# shogunの場合（起動に30秒以上、指示書読み込み含む）
./shutsujin_departure.sh
tmux attach -t shogun
> "このタスクを実行せよ"

# 現行フローの場合（即座に実行）
sessions_spawn(task="このタスクを実行せよ")
```

## shogunから学んだこと — 持ち帰るべき設計パターン

手動オーケストレーションは「負けた」が、設計思想には学ぶべき点があった。

### 1. 単一責任による競合防止

dashboard.mdの更新者を家老だけに限定した設計は、分散システムにおける「ライターは1つ」の原則そのものだ。マルチエージェント環境では、**誰が何を書くかを明示的に制約する**ことが安定性の鍵になる。

### 2. 構造化されたエスカレーション

「足軽はblocked報告を上げるだけ、判断は上位に委ねる」という設計は、エージェント数が増えた際にスケールする。現行の「司令塔が全部判断」方式は、エージェント数が少ないうちは最適だが、スケールには限界がある。

### 3. ペルソナの使い分け

足軽がタスクに応じて「シニアエンジニア」「テクニカルライター」とペルソナを切り替える設計は面白い。「戦国風の挨拶だけで、コードはプロ品質」という分離は、AIエージェントの出力品質を上げる実用的なテクニックだ。

### 4. スキル化候補の自動検出

足軽が毎回の報告で「この作業はスキル化できるか？」を自己判断する仕組みは、ナレッジマネジメントの自動化として応用できる。

## 結論

マルチエージェントオーケストレーションは、**ツールの進化速度が自作の改良速度を上回る**領域だ。2週間前に「必要だ」と思って作った仕組みが、プラットフォームの進化で不要になる。

だからといって「作るな」ではない。**自作したからこそ、統合プラットフォームの何がありがたいかが分かる。** send-keysの罠を踏んだからこそ、API通信の安定性に感謝できる。レースコンディションと戦ったからこそ、隔離セッションの価値が理解できる。

マルチエージェント開発を始める人へのアドバイス：

1. **まず統合プラットフォームを使え** — 手動構築は学習目的以外では非推奨
2. **ただし設計パターンは学べ** — 単一責任、構造化エスカレーション、非同期ステータス共有
3. **撤退を恐れるな** — 2週間で畳む判断は正しい。沈没コストに囚われるな

> 「勝敗は兵家の常」—— 重要なのは、負けから何を学ぶかだ。

## 関連記事

- [AIに仕事を任せる技術 — AI司令塔×マルチエージェント実践](https://zenn.dev/imudak/articles/ai-autonomous-dev-practice)
- [multi-agent-shogun（fork元）](https://github.com/yohey-w/multi-agent-shogun)
